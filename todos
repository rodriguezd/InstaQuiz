todos

/login => sessions#new
/sessions => sessions#create
/logout => session#destroy
/register => users#new
POST /users => users#create

question can only be added by the creator of a quiz
quizzes must be linked to a user
we need to lock down the controller action that adds questions to a quiz
have a way to return true/false if a user can edit a quiz
