Hey, <%= @user %>.

The <%= @quiz.name %> quiz was just created! Log in and submit questions for it.

Here are the directions:

<%= @quiz.instruction %>

<%= site_landing_url(:host => 'localhost:3000') %>

Have a great day!